# 期待: 【方針次第】
#   A（ドキュメントは 1 つだけ）を採るならエラー
#   B（空の 2 つ目は許す）を採るなら成功 / Programs に p が 1 件
# 狙い: 末尾の --- は「空の 2 つ目のドキュメント」になる（実測: 2 回目の
#   Decode が err=nil, 中身が空で返る）。invalid_multiple_documents.yaml と
#   同じ経路に乗るので、厳しさをどこで切るかがここで決まる
programs:
  p:
    cmd: /bin/true
---
