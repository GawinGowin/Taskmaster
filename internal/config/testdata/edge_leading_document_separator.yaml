# 期待: 成功 / Programs に p が 1 件
# 狙い: 先頭の --- は「ここからドキュメントが始まる」の明示であって、
#   2 つ目のドキュメントを作らない（実測: 1 回目 err=nil + 中身、2 回目 io.EOF）。
#   複数ドキュメント検出を「--- という文字列の有無」で実装すると、
#   この正常なファイルを誤って弾く。数えるのは Decode の回数であって行ではない
---
programs:
  p:
    cmd: /bin/true
